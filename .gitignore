.DS_Store
annunci_visti.backup-*.json

# Local-only secrets — never commit
.env
.env.*

# Local state from Claude Code harness (scheduler locks, etc.)
.claude/

# Python cache
__pycache__/
*.pyc

# Documenti personali (valutazioni, decisioni, ecc. — restano locali)
documents/
